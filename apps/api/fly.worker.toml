app = "verirule-worker"
primary_region = "lhr"

[build]
  dockerfile = "Dockerfile"

[processes]
  worker = "python -m app"

[env]
  VERIRULE_MODE = "worker"

# No http_service because worker does not serve traffic.
