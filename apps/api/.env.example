# FastAPI backend env (examples only — no real secrets)

# App
VERIRULE_ENV=development
API_HOST=127.0.0.1
API_PORT=8000

# Supabase (server-side usage)
# SUPABASE_URL is required.
# SUPABASE_ISSUER and SUPABASE_JWKS_URL are optional overrides.
SUPABASE_URL=
SUPABASE_ISSUER=
SUPABASE_JWKS_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Outbound notifications / integrations (future)
# EMAIL_PROVIDER_API_KEY=
# SLACK_WEBHOOK_URL=
